name: Generate changelog

on:
  pull_request:
    types: [closed]
  issues:
    types: [closed]
  workflow_run:
    workflows: ["Create release"]
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  generate-changelog:
    runs-on: ubuntu-latest
    # The workflow_run guard also filters on the upstream event: `Create release`
    # runs twice per release — once as a workflow_dispatch that only seeds the
    # container image, and once on the tag push that publishes the release.
    # Only the second one has a release to write a changelog for.
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push') ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Generate changelog
        uses: somaz94/go-changelog-action@v1
        with:
          output_file: CHANGELOG.md
          include_non_conventional: false

      - name: Git Commit and Push
        uses: somaz94/go-git-commit-action@v1
        with:
          branch: main
          commit_message: "docs: update changelog"
          user_email: "actions@github.com"
          user_name: "GitHub Actions"
          file_pattern: "CHANGELOG.md"
          github_token: ${{ secrets.GITHUB_TOKEN }}
